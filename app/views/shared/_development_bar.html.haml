#development_bar
  DEVELOPMENT MODE
  
  = form_tag development_session_path do
    Login as
    = select_tag 'user_id', options_for_select(User.all.map{|user| ["#{user.name} (#{user.email})", user.id] })
    = submit_tag 'Sign in'
