%h1 AWE Mgmt

- if user_signed_in?
  %h2
    Welcome,
    = current_user.name

- else
  %h2 OpenID Login
  = form_for :user, :url => user_session_path do |f|
    = f.text_field :identity_url, value: 'openid.tzi.org/'
    = f.submit "Sign in"
