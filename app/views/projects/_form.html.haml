= simple_form_for [parent, resource], html: {class: 'form-vertical'} do |f|
  = f.association :course
  = f.input :name
  = f.input :description
  
  .form-actions
    = f.button :submit, class: 'btn-primary'
