%h2= @page_title = 'Courses'

- if admin?
  %p= link_to "add course", new_resource_path

%table
  %thead
    %th Name
    %th.numeric Projects
    %th.numeric Groups
    %th.numeric Users
  %tbody
    = render collection
